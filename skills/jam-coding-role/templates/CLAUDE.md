# Claude Code adapter

Repository root `AGENTS.md` is canonical. Read it and the routed `.ai/*` files before acting.

This file defines no independent coding role. Use Claude-specific tools only as runtime capabilities under `.ai/WORKFLOW.md`. When a capability such as multi-agent delegation is unavailable, use the lowest sufficient single-agent route rather than emulating a missing team protocol.

Do not duplicate model settings, project invariants, memory rules, verification rules, or approval policy here.
